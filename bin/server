#!/usr/bin/ruby

if ARGV[0].include? "UP"
    # UP
    `python3 -m http.server 3000 &`
elsif ARGV[0].include? "DOWN"
    # DOWN
    `killall python3`
else
    `echo "Usage: SERVER UP | DOWN"`
end

`&`